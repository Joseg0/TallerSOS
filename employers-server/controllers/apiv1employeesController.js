import * as service from '../services/apiv1employeesService.js';

export function funcapiv1employees(req, res) {
    service.funcapiv1employees(req, res);
}

export function getEmployees(req, res) {
    service.getEmployees(req, res);
}

